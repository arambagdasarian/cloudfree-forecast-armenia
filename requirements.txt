# Core (required for run_pipeline.py --synthetic and make_figures.py)
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
matplotlib>=3.7

# Optional: if installed, the model uses LightGBM (matching the operational
# model in the paper) instead of scikit-learn's HistGradientBoosting fallback.
# lightgbm>=4.0

# forecast_live.py uses only the Python standard library (urllib); no extra
# dependency is needed for the live Open-Meteo forecast.
